//String, number, undefined, null, boolean | São imutáveis
const nome = 'Diogo'; //string
const nome1 = "Diogo"; //string
const nome2 = `Diogo`; //string 

const num1 = 10;    //number
const num2 = 10.52; //number

let nomeAluno; //undefined = não aponta para local nenhum na memória
const sobrenomeAluno = null; // nulo = não aponta para local nenhum na memória 

const boolean = true; //somente true ou false
const aprovado = true; //aprovado = true ou reprovado = false | boolean = true or false (Sistema Lógico)

console.log(typeof nome, nome);
console.log(typeof num1, num1);
console.log(typeof nomeAluno, nomeAluno);
console.log(typeof sobrenomeAluno, sobrenomeAluno);
console.log(typeof aprovado, aprovado);

